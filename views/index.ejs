<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <title>ChatApp</title>
</head>
<body>
    <% if (session.user) { %>
        <%- include('partials/userNavbar', {page: ''}) %>
    <% } else { %>
        <%- include('partials/navbar', {page: ''}) %>
    <% } %>
    <div class="h-screen">
        <div
            class="hero h-full bg-base-200"
        >
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Hello There</h1>
                    <p class="py-6">
                        Stay connected to your peers and friend thru the chatting application
                    </p>
                </div>
            </div>
        </div>
    </div>
    <%- include('partials/footer') %>
</body>
</html>